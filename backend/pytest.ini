[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -ra -q --strict-markers --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=95
filterwarnings =
    ignore::DeprecationWarning
